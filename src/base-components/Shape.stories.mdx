import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Meiuca } from '../../build/css/colors'

import Shape from './Shape.vue'

<Meta title="BaseComponents/Shape" component={Shape} />

# Shape
O `Shape` é um componente utilizado como uma casca para componentes de conteúdos, como por exemplo, o shape de um Card Component.
## Propriedades

<ArgsTable of={Shape} />

## Variações

### Estado padrão

export const Template = (args, { argTypes }) => ({
     props: Object.keys(argTypes),
    components: { Shape },
    template: `
      <Shape v-bind="$props">
      <slot>
          <div></div>
        </slot>
      </Shape>
      `
    });


Exemplo de uso default do componente de Shape.

<Story 
  name="Default"
  args={{ 
  }}>
  {Template.bind({})}
</Story>

### Estilos de brand dinâmicos

Neste stories, a ideia é trazer o exemplo do uso de styles dinâmicos via props, 
que podem também serem alterados aqui no Storybook via controles.
Os estilos dinâmicos que podem ser alterados são todos aqueles da folha de tokens de brand.
<Story
  name="Other"
  args={{ 
    backgroundColor: `${Meiuca.neutralColor4}`,
  }}>
  {Template.bind({})}
</Story>