import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Meiuca } from '../../build/css/colors'

import Button from './Button.vue'

<Meta title="BaseComponents/Button" component={Button} />

# Button
O `Button` trata dos comportamentos básicos de um botão: cores, e estados de *hover*.
## Propriedades

<ArgsTable of={Button} />

## Variações

### Estado padrão

export const Template = (args, { argTypes }) => ({
     props: Object.keys(argTypes),
    components: { Button },
    template: `
      <Button v-bind="$props"></Button>
      `
    });


Exemplo de uso default do componente de botão, com controles para (`label`).

<Story 
  name="Default"
  args={{ 
    label: 'Button Label'
  }}>
  {Template.bind({})}
</Story>

### Estilos de brand dinâmicos

Neste stories, a ideia é trazer o exemplo do uso de styles dinâmicos via props, 
que podem também serem alterados aqui no Storybook via controles.
Os estilos dinâmicos que podem ser alterados são todos aqueles da folha de tokens de brand.
<Story 
  name="Other"
  args={{ 
    label: 'Button',
    backgroundColor: `${Meiuca.brandColorPrimary1}`,
    hoverColor: `${Meiuca.brandColorPrimary2}`,
    color: `${Meiuca.neutralColor5}`,
    fontWeight: `${Meiuca.fontWeightRegular}`
  }}>
  {Template.bind({})}
</Story>