import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Meiuca } from '../../build/css/colors'

import Heading from './Heading.vue'

<Meta title="BaseComponents/Heading" component={Heading} />

# Heading
O `Heading` é um componente básico para títulos em conteúdos e páginas.
## Propriedades

<ArgsTable of={Heading} />

## Variações

### Estado padrão

export const Template = (args, { argTypes }) => ({
     props: Object.keys(argTypes),
    components: { Heading },
    template: `
      <Heading v-bind="$props"></Heading>
      `
    });


Exemplo de uso default do componente de heading, com controles para (`heading`).

<Story 
  name="Default"
  args={{ 
    heading: 'Conteúdo do heading'
  }}>
  {Template.bind({})}
</Story>

### Estilos de brand dinâmicos

Neste stories, a ideia é trazer o exemplo do uso de styles dinâmicos via props, 
que podem também serem alterados aqui no Storybook via controles.
Os estilos dinâmicos que podem ser alterados são todos aqueles da folha de tokens de brand.
<Story
  name="Other"
  args={{ 
    heading: 'Heading com estilo dinâmico',
    color: `${Meiuca.neutralColor4}`,
    fontWeight: `${Meiuca.fontWeightMedium}`
  }}>
  {Template.bind({})}
</Story>